const express = require('express');
const { sayhello, orderWebhookHandler } = require('../controller/orderController');
const orderRoutes = express.Router();


orderRoutes.post('/create',orderWebhookHandler);


module.exports = orderRoutes;

































































